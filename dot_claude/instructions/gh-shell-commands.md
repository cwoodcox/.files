## Prefer GitHub MCP tools over `gh`

When interacting with GitHub, prefer the available GitHub MCP tools over `gh`. MCP tools are more reliable and return structured data.

Use `gh` only when:
- No suitable GitHub MCP tool exists, or
- The task requires `gh`-specific functionality, such as `gh auth`, `gh extension`, or raw API calls not covered by tools.
