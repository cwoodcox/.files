# About this account

Everything on this Claude account is personal — no employer/work projects. Treat all repos here as the user's own—including ones that look organizational—unless they are larger open-source projects .

# Reference notes (read on demand)

Topic-scoped knowledge lives in `~/.claude/notes/` and is *not* auto-loaded — read the relevant file only when the current task is in scope. Keep this index to one line per file.

- `notes/cloudflare-workers.md` — Cloudflare Workers preview URLs (per-branch + per-version) for testing PR candidates without promoting to prod
